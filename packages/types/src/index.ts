export type ApiError = { error: { code: string; message: string } };
export type BlockRef = { block_number: number; transaction_hash: string; log_index: number };
export type TokenMetrics = { trade_count: number; buy_count: number; sell_count: number; volume: string; fees: string; unique_trader_count: number; latest_trade_timestamp: number | null; current_price: string | null; fully_diluted_value: string | null; holder_count: number | null };
export type Graduation = { phase: string; canonical_pool_address?: string; graduation_manager_address?: string; lp_custodian_address?: string; position_token_id?: string; liquidity?: string; token_amount?: string; native_usdc_amount?: string; sold_supply?: string; curve_terminal_at?: BlockRef; settled_at?: BlockRef; liquidity_token?: string; quote_amount?: string; liquidity_amount?: string; lock_id?: string; unlock_timestamp?: number };
export type Token = { address: string; creator: string; name: string; symbol: string; initial_supply: string; description?: string; image_url?: string; metadata_url?: string; website_url?: string; x_url?: string; telegram_url?: string; discord_url?: string; created_at: BlockRef; metrics: TokenMetrics; curve?: { address: string; canonical_pool_address?: string; sold_supply: string; reserve_balance: string; supply?: string; starting_price?: string; slope?: string; graduation_threshold?: string; lifecycle?: string }; graduation?: Graduation };
export type TokenPage = { items: Token[]; next_cursor?: string };
export type Trade = { token_address: string; trader: string; side: string; token_amount: string; reserve_amount: string; curve_value: string; protocol_fee: string; creator_fee: string; source: "curve" | "uniswap_v3"; block_number: number; transaction_index: number; transaction_hash: string; log_index: number };
export type Activity = { event_name: string; decoded: Record<string, unknown>; block_number: number; transaction_index: number; transaction_hash: string; log_index: number };
export type TradePage = { items: Trade[]; next_cursor?: string };
export type ActivityPage = { items: Activity[]; next_cursor?: string };
export type ChartPoint = { bucket_start: number; trade_count: number; buy_count: number; sell_count: number; volume: string; unique_trader_count: number; open_price: string | null; high_price: string | null; low_price: string | null; close_price: string | null };
export type ChartInterval = "1m" | "5m" | "15m" | "1h" | "4h" | "1d" | "1w";
export type ChartPage = { interval: ChartInterval; supported_intervals: ChartInterval[]; candles: ChartPoint[] };
export type Pricing = { token_address: string; current_price: string | null; fully_diluted_value: string | null; source: "indexed_v3_curve" | "indexed_v3_market" };
export type ETHUSDPrice = { price: string; price_decimals: 8; updated_at: string; feed: string; source: "chainlink_eth_usd"; max_age_seconds: number };
export type CreatorProfile = { address: string; token_count: number; volume: string; tokens: Token[]; next_cursor?: string };
export type IndexedProvenance = { block_number: number; block_hash: string; transaction_hash: string; log_index: number };
export type CTOStatus = { chain_id: number; token: string; active: boolean; registry?: string; treasury?: string; controller?: string; previous_recipient?: string; active_proposal_id?: string; activation?: IndexedProvenance };
export type CTOProposal = { proposal_id: string; token: string; registry: string; treasury: string; creator: string; controller: string; previous_recipient: string; nonce: string; metadata_hash: string; metadata_uri: string; state: string; created_timestamp: number; created: IndexedProvenance; acceptance_deadline: number; accepted_at?: number; accepted?: IndexedProvenance; execute_after?: number; execute_deadline?: number; ready?: IndexedProvenance; cancelled_at?: number; cancelled?: IndexedProvenance; expired_at?: number; expired?: IndexedProvenance; activated_at?: number; activated?: IndexedProvenance };
export type CTOProposalPage = { items: CTOProposal[]; next_cursor?: string };
export type CTOSupportedAsset = { asset: string; controller: string; registered: IndexedProvenance };
export type CTOTreasuryTransfer = { asset: string; recipient: string; amount: string; controller: string; provenance: IndexedProvenance };
export type CTOFeePull = { token: string; asset: string; amount: string; triggered_by: string; provenance: IndexedProvenance };
export type CTOTreasuryTransferPage = { items: CTOTreasuryTransfer[]; next_cursor?: string };
export type CTOFeePullPage = { items: CTOFeePull[]; next_cursor?: string };
export type CTOTreasury = { treasury: string; registry: string; token: string; controller: string; canonical_usdc?: string; nonce: string; deployment: IndexedProvenance; supported_assets: CTOSupportedAsset[]; recent_transfers: CTOTreasuryTransfer[]; transfers_next_cursor?: string; recent_fee_pulls: CTOFeePull[]; fee_pulls_next_cursor?: string };
export type CTOCheckpointAggregate = { token: string; recipient: string; checkpointed: string; claimed: string; outstanding: string };
export type CTOCheckpointEvent = { recipient: string; action: string; amount: string; triggered_by?: string; provenance: IndexedProvenance };
export type CTOCheckpointPage = { token: string; aggregates: CTOCheckpointAggregate[]; items: CTOCheckpointEvent[]; next_cursor?: string };
