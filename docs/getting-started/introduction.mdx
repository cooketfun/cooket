---
title: "Introduction"
description: "How Cooket fits together and what it does."
---

Cooket combines an onchain launch and trading protocol with a web interface and an indexed data service. The browser wallet signs every state-changing action. The web application never calculates authoritative balances or reserves; it reads contracts for transaction-critical state and uses the API for indexed discovery and history.

## What you can do

| Action | Where it happens | Confirmation |
| --- | --- | --- |
| Browse tokens and activity | Indexed web data | None |
| Launch a token | `CooketFactoryV3` | Browser wallet transaction |
| Buy or sell on the curve | Per-token `CooketCurveV3` | Browser wallet transaction |
| Swap a graduated token | Uniswap V3 periphery (execution deferred) | Browser wallet transaction |
| Verify activity | ArcScan | None |

The protocol creates a fixed-supply ERC-20 and a dedicated curve for each launch. The token trades on that curve until the precise terminal condition is reached. Curve reserve and graduation principal are 18-decimal native USDC, not ETH. If the terminal condition is reached, the contract implements an atomic transition into a pre-reserved canonical token/native-USDC Uniswap V3 pool. Existing graduated-swap execution in this repository remains deferred. See [Graduation](/docs/protocol/graduation).

<Note>
  “Permissionless” means the launch function does not require approval. It does not mean every token is trustworthy, valuable, or likely to graduate.
</Note>
